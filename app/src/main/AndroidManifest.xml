<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.milenal.listoapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ForgotPassword" />
        <activity android:name=".activity.Register" />
        <activity android:name=".activity.Planner" />
        <activity android:name=".activity.Songbook" />
        <activity android:name=".activity.ProgressionScout" />
        <activity android:name=".activity.ProgressionWolf" />
        <activity android:name=".activity.ProgressionSenior" />
        <activity android:name=".activity.ProgressionRoover" />
        <activity android:name=".activity.Library" />
        <activity android:name=".activity.Book"></activity>
    </application>

</manifest>